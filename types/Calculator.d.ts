export type LoanData = {
  amount: number,
  term: number,
  rate: number,
  date: string,
}

export type TableRow = {
  amount: number,
  interest: number,
  principal: number,
  ending: number,
  date: string,
}